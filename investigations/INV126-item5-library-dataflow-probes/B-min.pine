//@version=6
indicator("Bmin", overlay = true)
highSource = high
lowSource = low
score() =>
    s = 0
    for bar = bar_index - 10 to bar_index by 1
        if highSource[bar_index - bar] > lowSource[bar_index - bar]
            s := s + 1
    s
control = close > open ? ta.sma(close, 5) : 0.0
x = 0
for i = 0 to 2 by 1
    x := score()
plot(x + control)
