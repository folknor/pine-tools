//@version=6
// Probe (pine-lint --tv, 2026-06-25): CE10123 @ 7:15 -
// "Cannot call array.push with argument value=1.5. An argument of literal
//  float type was used but a series int is expected."
indicator("s")
a = array.new<int>()
array.push(a, 1.5)
plot(array.size(a))
