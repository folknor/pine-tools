// @test validation/logical-operators
// @description Boolean types in logical operators (and/or)
// @expects parse: success
// @expects no-errors

//@version=6
indicator("Logical Operators Tests")

// Basic boolean operations
bool test1 = true and false
bool test2 = true or false
bool test3 = not true

// Comparison results with logical operators
bool cmp1 = (close > open) and (high > low)
bool cmp2 = (close > open) or (high > low)
bool cmp3 = not (close > open)

// Series bool operations
bool seriesTest1 = (close > open) and (volume > 0)
bool seriesTest2 = (close > open) or (volume > 0)

// Chained boolean operations
bool chained1 = true and false and true
bool chained2 = true or false or true
bool chained3 = (close > open) and (high > low) and (volume > 0)

// In conditional expressions
result = (close > open) and (volume > 0) ? high : low

// In if conditions
if (close > open) and (volume > 0)
    label.new(bar_index, high, "Active")

// Negation with comparison
bool neg1 = not (close == open)
bool neg2 = not na(close)

plot(close)
