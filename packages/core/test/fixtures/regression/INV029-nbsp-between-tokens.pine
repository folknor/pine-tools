// @test regression/INV029-nbsp-between-tokens
// @description A mid-line U+00A0 (non-breaking space) is a lexical error in TV: CE10005 "no viable alternative at character" anchored at the NBSP itself (probed 2026-06-04, see investigations/INV029-nbsp-between-tokens/notes.md). The line below has an NBSP between the = and the value. NBSP as leading indentation stays accepted (our lenient reading; TV wrap-joins such lines instead - see the INV).
// @expects parse: fail
// @expects error: line=2, column=4, message=/no viable alternative at character/
//@version=6
x = close
plot(x)
