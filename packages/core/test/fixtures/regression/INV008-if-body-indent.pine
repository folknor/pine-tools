// @test regression/INV008-if-body-indent
// @description A column-1 leading operator must not be treated as the body of a prior bodyless block. TV reports CE10156 at the operator line; subsequent same-column statements stay at global scope. See investigations/INV008-if-body-indent-leak/notes.md and INV078.
// @expects parse: fail
// @expects errors: 1
// @expects error: line=4, message=/Syntax error at input "new line"/
//@version=6
indicator("t")

or rename if conflict

overlap = input.bool(true)
plot(close)
