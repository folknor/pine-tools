// @test syntax/for-loops
// @description For loop variations including step syntax
// @expects parse: success
// @expects no-errors

//@version=6
indicator("For Loop Tests")

// Basic for loop
sum1 = 0.0
for i = 0 to 10
    sum1 += i

// For loop with step (by keyword)
sum2 = 0.0
for i = 0 to 10 by 2
    sum2 += i

// For loop with negative step
sum3 = 0.0
for i = 10 to 0 by -1
    sum3 += i

// For loop with expressions
len = 14
sum4 = 0.0
for i = 0 to len - 1
    sum4 += close[i]

// Nested for loops
matrix = 0.0
for i = 0 to 3
    for j = 0 to 3
        matrix += i * j

plot(sum1)
