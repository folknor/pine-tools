// @test syntax/while-loops
// @description While loop syntax
// @expects parse: success
// @expects no-errors

//@version=6
indicator("While Loop Tests")

// Simple while loop
var count1 = 0
i = 0
while i < 10
    count1 += 1
    i += 1

// While with break
var count2 = 0
j = 0
while true
    count2 += 1
    j += 1
    if j >= 5
        break

// While with continue
var sum = 0
k = 0
while k < 10
    k += 1
    if k % 2 == 0
        continue
    sum += k

plot(count1 + count2 + sum)
