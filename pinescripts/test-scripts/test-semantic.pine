//@version=6
indicator("Semantic Test")

// This should trigger CONDITIONAL_SERIES warning
if close > open
    ta.sma(close, 14)  // ta.* function inside conditional

// This should trigger CONDITIONAL_REASSIGNMENT warning  
var float testVar = 0.0
if close > open
    testVar := close  // reassignment inside conditional

// This should be fine
float normalVar = ta.sma(close, 14)