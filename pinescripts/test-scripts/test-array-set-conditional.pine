//@version=6
indicator("Array Set Conditional Test")

// Test function with conditional ta.sma call as argument to array.set
testFunction() =>
    if bar_index == 0
        array.set(tot_chg_avg, count, ta.sma(abs_change, count))  // This should trigger CONDITIONAL_SERIES warning

// Call the function
testFunction()