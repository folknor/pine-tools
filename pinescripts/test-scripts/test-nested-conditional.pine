//@version=6
indicator("Nested Conditional Test")

// Test function with conditional ta.sma call
testFunction() =>
    if bar_index == 0
        ta.sma(close, 14)  // This should trigger CONDITIONAL_SERIES warning

// Call the function
testFunction()